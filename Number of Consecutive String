#include<stdio.h>

int main() {
 int count=0,temp,i,pos,max,n;
 scanf("%d",&n);
 int *a=malloc(sizeof(int)*n);
 for(i=0;i<n;i++)
 scanf("%d",&a[i]);
 for(i=0;i<n;i++)
 {
     if(a[i]!=a[i+1]-1)
     {
         count++;
         
     }
     
 }
 printf("%d",count);
 return 0;
}
